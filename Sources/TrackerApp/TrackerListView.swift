import SwiftUI

struct TrackerListView: View {
    var body: some View {
        NavigationView {
            Text("Hello, Tracker!")
                .navigationTitle("Tracker App")
        }
    }
}
